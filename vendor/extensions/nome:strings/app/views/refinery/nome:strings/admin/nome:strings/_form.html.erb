<%= form_for [refinery, :nome:strings_admin, @nome:string] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @nome:string,
              :include_object_name => true %>

  
  <div class='field'>
    <%= f.label :modelo -%>
    <%= f.text_field :modelo, :class => 'larger widest'  -%>

  </div>
  
  <div class='field'>
    <%= f.label :fabricante -%>
    <%= f.text_field :fabricante  -%>

  </div>
  
  <div class='field'>
    <div id='page-tabs' class='clearfix ui-tabs ui-widget ui-widget-content ui-corner-all'>
      <ul id='page_parts'>
        <% [:especificacao].each_with_index do |part, part_index| %>
          <li class='ui-state-default<%= ' ui-state-active' if part_index == 0 %>'>
            <%= link_to t("#{part}", :scope => "activerecord.attributes.refinery/nome:strings/nome:string", :default => part.to_s.titleize), "##{part}" %>
          </li>
        <% end %>
      </ul>
      <div id='page_part_editors'>
        <% [:especificacao].each do |part| %>
          <div class='page_part' id='<%= part %>'>
            <%= f.text_area part, :rows => 20, :class => 'wymeditor widest' -%>
          </div>
        <% end %>
      </div>
    </div>

  </div>
  
  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.nome:strings.admin.nome:strings.nome:string'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @nome:string.modelo) %>
<% end -%>
<% content_for :javascripts do %>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end %>
